# reactstrap@8 declares a React 16 peer dependency, but this project runs on
# React 18 (the pairing works fine at runtime). Tell npm to install with the
# legacy peer-dependency resolver so CI/CD (e.g. Netlify) doesn't abort with
# ERESOLVE the way `npm install` does by default.
legacy-peer-deps=true
